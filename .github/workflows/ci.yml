name: CI

on:
  pull_request:
    branches: [main]

jobs:
  web-client:
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: sk-placeholder-ci
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci
      - run: npm run check --workspace web-client
      - run: npm test --workspace web-client

  auth-system:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: auth-system
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 25
          cache: gradle
      - run: ./gradlew test
