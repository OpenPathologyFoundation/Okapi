# Starling Auth System Environment Variables
# Copy to .env and fill in values. Never commit the .env file.

# ── OIDC Configuration ──────────────────────────────────────────────
# Set OIDC_ISSUER_URI to your Keycloak realm URL. All OIDC endpoints
# are derived from it automatically. Override individual endpoints only
# if your IdP uses non-standard paths.
OIDC_ISSUER_URI=http://localhost:8180/realms/starling
OIDC_CLIENT_ID=starling-client
OIDC_CLIENT_SECRET=starling-secret
# Optional overrides (usually not needed):
# OIDC_AUTHORIZATION_URI=
# OIDC_TOKEN_URI=
# OIDC_USER_INFO_URI=
# OIDC_JWK_SET_URI=
# OIDC_END_SESSION_URI=

# ── Keycloak Admin API ────────────────────────────────────────────────
# Used by seed endpoint to validate users against Keycloak
STARLING_KEYCLOAK_ADMIN_PASSWORD=admin

# ── Database Configuration ───────────────────────────────────────────
# In production, these should be loaded from Secrets Manager
POSTGRES_DB=starling_auth
POSTGRES_USER=starling_service
POSTGRES_PASSWORD=change_me_locally
POSTGRES_PORT=5432

# ── JWT Signing (REQUIRED — change for any non-local deployment) ─────
# Must be at least 32 characters for HMAC-SHA256
STARLING_JWT_SECRET=change-me-at-least-32-characters-long
STARLING_JWT_ISSUER=starling
STARLING_JWT_AUDIENCE=starling-tile-server
STARLING_JWT_TTL_SECONDS=600

# ── Viewer Integration (Optional) ───────────────────────────────────
# Comma-separated origins for the viewer window when NOT behind nginx proxy
# Example: http://localhost:5174,http://viewer.starling.local
# STARLING_VIEWER_ALLOWED_ORIGINS=

# ── Server (Optional) ───────────────────────────────────────────────
# SERVER_PORT=8080

# ── SAML Configuration (Optional — uncomment to enable) ─────────────
# SAML_IDP_METADATA_URL=https://dev-123456.okta.com/app/exk.../sso/saml/metadata
